<p><strong><%= reply.user.email %></strong> : <%= reply.body %></p>

<p><%= link_to 'Add a Reply', new_post_comment_path(@post, parent_id: reply.id), remote: true %>
| <%= link_to 'Delete Reply', [reply.post, reply], data: {
                turbo_method: :delete,
                turbo_confirm: "Are you sure?"
              } %></p>

<div class= "ps-3">
  <% if reply.replies.any? %>
    <% reply.replies.each do |reply| %> 
 <%= render 'comments/reply', reply: reply %> 
 <br>
    <% end %>
  <% end %>
  </div>